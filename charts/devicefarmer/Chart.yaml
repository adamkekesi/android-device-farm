apiVersion: v2
name: devicefarmer
description: |
  DeviceFarmer (STF) control plane for the Android Device Farm: RethinkDB, the
  triproxies, processor, app, auth, api, websocket, storage plugins, reaper, and
  ingress. Built from DeviceFarmer's docker-compose topology (no maintained
  official chart exists). See docs/IMPLEMENTATION_PLAN.md §1 (Phase 1).
type: application

# Chart version — bump on chart changes.
version: 0.1.0
# Tracks the DeviceFarmer/STF release this chart targets.
appVersion: "3.7.7"

keywords:
  - android
  - stf
  - devicefarmer
  - device-farm
home: https://github.com/DeviceFarmer/stf
