---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: deviceleases.farm.example.com
spec:
  group: farm.example.com
  names:
    kind: DeviceLease
    listKind: DeviceLeaseList
    plural: deviceleases
    shortNames:
    - dfl
    singular: devicelease
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.classRef
      name: Class
      type: string
    - jsonPath: .status.deviceRef
      name: Device
      type: string
    - jsonPath: .spec.requester
      name: Requester
      type: string
    - jsonPath: .status.expiresAt
      name: Expires
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DeviceLease is the Schema for the deviceleases API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DeviceLease
            properties:
              classRef:
                description: ClassRef is the requested DeviceClass.
                type: string
              poolRef:
                description: PoolRef optionally pins the lease to a specific DevicePool.
                type: string
              requester:
                description: Requester is an opaque identifier (CI job id, user).
                type: string
              ttlSeconds:
                default: 300
                description: TTLSeconds is the lease lifetime; refreshed by heartbeats.
                format: int32
                minimum: 1
                type: integer
            required:
            - classRef
            - requester
            - ttlSeconds
            type: object
          status:
            description: status defines the observed state of DeviceLease
            properties:
              adbEndpoint:
                description: ADBEndpoint is the bound device's adb endpoint.
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deviceRef:
                description: DeviceRef is the bound Device (namespaced name).
                type: string
              expiresAt:
                description: ExpiresAt is when the lease expires absent a heartbeat.
                format: date-time
                type: string
              lastHeartbeat:
                description: LastHeartbeat is the last time the lease was refreshed.
                format: date-time
                type: string
              phase:
                description: Phase is the lease lifecycle phase.
                enum:
                - Pending
                - Bound
                - Expired
                - Released
                - Failed
                type: string
              uiEndpoint:
                description: UIEndpoint is the bound device's optional viewer URL.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
